创建项目：
Vite是Vue作者开发的一款意图取代webpack的工具
npm install -g create-vite-app
create-vite-app xxx项目名

或者
npm init vite-app <project-name>
 cd <project-name>
 npm install
 npm run dev

Vue3的变化：
组合API
diff算法性能提升，做记号，需要更新的就更新，不做全量更新
还有一点区别就是导入了内容没使用, 不会报错

Vue2可以认为是选项式api
Vue3则是则合式api, 注意setup中没有this
